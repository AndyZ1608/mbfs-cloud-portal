services:
  mbfs-cloud-portal:
    build: .
    container_name: mbfs-cloud-portal
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - portal-data:/data

volumes:
  portal-data:
